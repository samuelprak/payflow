name: Backend tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: payflow
          POSTGRES_PASSWORD: payflow123
          POSTGRES_DB: payflow
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U payflow -d payflow"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout main
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Install dependencies
        run: npm install

      - name: Format code
        run: npm run format:check --ws

      - name: Lint code
        run: npm run lint:check --ws

      - name: Run tests
        run: npm run test --ws
        env:
          DATABASE_URL: postgres://payflow:payflow123@localhost:5432/payflow
          REDIS_URL: redis://localhost:6379
